#pragma once

// Copy this file to secrets.h and fill in local values before building.
#define WIFI_SSID     "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
// Use the backend origin only, without a trailing slash or API path.
#define BACKEND_URL   "https://YOUR_BACKEND.up.railway.app"
